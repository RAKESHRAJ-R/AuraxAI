{
  "name": "theaurax",
  "version": "1.0.0",
  "private": true,
  "description": "Theaurax monorepo — apps/bot (WhatsApp AI sales bot + API) and apps/admin (React admin console, deployed to Vercel).",
  "scripts": {
    "bot": "npm --prefix apps/bot run start",
    "install:bot": "npm --prefix apps/bot install",
    "install:admin": "npm --prefix apps/admin install",
    "install:all": "npm run install:bot && npm run install:admin",
    "dev-admin": "npm --prefix apps/admin run dev",
    "build-admin": "npm --prefix apps/admin install && npm --prefix apps/admin run build:express"
  }
}
